name: flutter_svg_pro_example
description: "Example app for flutter_svg_pro"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  flutter_svg_pro:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  xml: ^6.5.0
  vector_math: ^2.1.0

flutter:
  uses-material-design: true

  assets:
    - assets/
